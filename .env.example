# Copy this file to .env and fill in real values before running the app.

# Postgres connection string for Prisma. Any Postgres works — Supabase,
# Railway, Neon, or a local `docker run postgres` instance.
DATABASE_URL="postgresql://neondb_owner:npg_MiDvKcU62faQ@ep-square-shape-ayl1c4nx-pooler.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
# Public base URL used to build QR verification links (lib/qrcode.ts).
NEXT_PUBLIC_BASE_URL="https://safeverse.app"

# Random secret used to sign login session cookies (lib/auth.ts).
# Generate one with: openssl rand -base64 32
SESSION_SECRET="replace-this-with-a-long-random-string"
